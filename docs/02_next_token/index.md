# 02 · 下一个 Token：为什么预测能产生智能

> 一句话直觉：**LLM 的表面任务是预测下一个 Token，但训练这个预测任务时，模型被迫学习了语言背后的结构。**

---

## 1. 最简单的问题

输入：

```text
今天天气很
```

模型需要预测：

```text
好
```

数学上，它计算的是：

```text
P(下一个Token | 已有Token)
```

也就是：

```text
给定前面的所有信息，下一个符号出现的概率。
```

---

## 2. 输出不是一个答案，而是一张概率表

模型最后一层会输出一个巨大向量：

```text
天气好   0.45
天气冷   0.20
天气热   0.18
天气差   0.05
...
```

然后根据策略选择一个 Token。

这也是为什么同一个问题，多次生成可能得到不同答案。

模型不是查数据库，而是在概率空间中生成。

---

## 3. 为什么这么简单的目标会产生复杂能力？

关键在训练数据。

假设训练目标只是：

```text
预测下一个字
```

但是为了预测准确，模型必须知道：

- 语法
- 词语关系
- 世界知识
- 长距离依赖
- 角色关系
- 常见逻辑模式

例如：

```text
爱因斯坦提出了
```

下一个 Token 很可能是：

```text
相对论
```

模型如果不知道爱因斯坦是谁，就很难预测。

因此：

> 预测任务成为了学习世界结构的压力来源。

---

## 4. 它不是背答案吗？

这是最常见的问题。

如果只是背诵：

```text
输入A
→ 固定输出B
```

那么面对没见过的新组合，模型应该失败。

但语言有大量组合关系。

训练过程中模型学习的是：

```text
哪些模式通常一起出现
哪些概念相互关联
哪些结构可以迁移
```

因此它可以生成训练数据里没有出现过的句子。

---

## 5. 一个重要误区：概率 ≠ 没有能力

很多人说：

“它只是概率预测。”

这句话数学上没错，但容易产生误解。

人脑也不是从一个神秘数据库里取答案。

神经系统同样是在已有经验和当前刺激之间进行预测。

区别在于：

LLM 的预测空间来自参数和上下文；人的预测系统来自生物神经系统。

---

## 6. 为什么规模很重要？

小模型：

```text
输入
 ↓
简单模式匹配
 ↓
输出
```

大模型：

```text
输入
 ↓
多层表示转换
 ↓
抽象模式
 ↓
上下文推理
 ↓
输出
```

规模增加后，模型可以存储和组合更多复杂模式。

---

## 7. 但它为什么会“突然变聪明”？

这涉及后面的训练章节。

参数不是一次性写入知识。

训练过程更像：

```mermaid
flowchart LR
A[随机参数] --> B[预测]
B --> C[和真实答案比较]
C --> D[计算误差 Loss]
D --> E[调整参数]
E --> B
```

循环几十亿次后，参数逐渐形成有用的表示。

---

## 本章总结

| 问题 | 答案 |
| --- | --- |
| LLM 在做什么 | 预测下一个 Token |
| 为什么不是简单补全 | 因为预测需要理解上下文结构 |
| 它有没有概率性 | 有 |
| 概率性代表没有能力吗 | 不代表 |
| 能力从哪里来 | 训练过程中参数逐渐形成 |

下一章：

> Transformer 如何让这些 Token 之间互相影响？

也就是：Attention。
